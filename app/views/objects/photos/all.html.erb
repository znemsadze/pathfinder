<h2 class="page-header"><%= @title %></h2>
<%= render partial: 'objects/photos/gallery', locals: { photos: @photos } %>
<%= will_paginate @photos %>
<p>
  სულ: <code><%= @photos.total_entries %></code> ფოტო
</p>